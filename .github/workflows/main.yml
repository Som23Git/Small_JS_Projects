name: Add hello_world.js
  

on:
  schedule:
    - cron: '30 6,7 * * *'
  push:
    branches:
      - main


jobs:
  add_file:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Create hello_world.js
      run: echo "console.log('Hello, world!')" > hello_world.js

    - name: Commit and push changes
      run: |
        git config --local user.email "somesh.rokz@gmail.com"
        git config --local user.name "Som23Git"
        git add .
        git commit -m "Added hello_world.js"
        git push origin main
